/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  // eslint: {
  //   ignoreDuringBuilds: true, // Optional: disable lint error on Vercel
  // },
};

module.exports = nextConfig;
